name: Sync Upstream and Trigger Build

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

env:
  UPSTREAM_REPO: XyhTender/qq-farm-automation-bot
  UPSTREAM_BRANCH: main
  LOCAL_BRANCH: main

jobs:
  sync-and-trigger:
    runs-on: ubuntu-latest
    outputs:
      should_build: ${{ steps.check.outputs.updates_available }}
      new_tag: ${{ steps.extract_tag.outputs.new_tag || '' }}
    steps:
      - name: Checkout current repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.TOKEN }}
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'

      - name: Add upstream remote
        run: |
          git remote add upstream https://github.com/${{ env.UPSTREAM_REPO }}.git
          git remote set-branches upstream '*'

      - name: Fetch upstream changes
        run: git fetch upstream

      - name: Check for updates
        id: check
        run: |
          LOCAL_SHA=$(git rev-parse HEAD)
          REMOTE_SHA=$(git rev-parse upstream/${{ env.UPSTREAM_BRANCH }})
          
          if [ "$LOCAL_SHA" != "$REMOTE_SHA" ]; then
            echo "Changes detected. Proceeding with merge and tag extraction."
            echo "updates_available=true" >> $GITHUB_OUTPUT
          else
            echo "No changes detected. No further action needed."
            echo "updates_available=false" >> $GITHUB_OUTPUT
            exit 78
          fi

      - name: Merge upstream changes (preferring upstream on conflicts)
        if: steps.check.outputs.updates_available == 'true'
        run: |
          git pull -X theirs upstream $UPSTREAM_BRANCH

      - name: Push merged changes to local main branch
        if: steps.check.outputs.updates_available == 'true'
        run: git push origin ${{ env.LOCAL_BRANCH }}

      - name: Extract tag from Update.log (root directory)
        id: extract_tag
        if: steps.check.outputs.updates_available == 'true'
        run: |
          LOG_FILE="./Update.log"
          
          echo "Current working directory: $(pwd)"
          echo "Files in current directory:"
          ls -la
          
          if [ -f "$LOG_FILE" ]; then
            echo "Update.log file found at path: $LOG_FILE"
            NEW_TAG=$(head -n 1 "$LOG_FILE" | grep -oE '[0-9]{4}/[0-9]{2}/[0-9]{2}' | head -1)
            
            if [ -n "$NEW_TAG" ]; then
              echo "Extracted tag from Update.log: $NEW_TAG"
              echo "new_tag=$NEW_TAG" >> $GITHUB_OUTPUT
            else
              echo "No valid date format (YYYY/MM/DD) found in Update.log"
              echo "new_tag=unknown" >> $GITHUB_OUTPUT
            fi
          else
            echo "Error: Update.log file not found in root directory"
            echo "new_tag=no_log" >> $GITHUB_OUTPUT
          fi
          
      - name: Print sync summary
        if: steps.check.outputs.updates_available == 'true'
        run: |
          echo "Repository synced with upstream using 'theirs' strategy on conflicts."
          echo "Triggering build with tag: ${{ steps.extract_tag.outputs.new_tag }}"

  trigger-docker-build:
    needs: sync-and-trigger
    if: needs.sync-and-trigger.result == 'success' && needs.sync-and-trigger.outputs.should_build == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Docker Build Workflow
        if: needs.sync-and-trigger.outputs.new_tag != ''
        run: |
          curl -X POST \
            -H "Accept: application/vnd.github.v3+json" \
            -H "Authorization: token ${{ secrets.TOKEN }}" \
            https://api.github.com/repos/${{ github.repository }}/actions/workflows/docker-image.yml/dispatches \
            -d '{"ref":"${{ github.ref_name }}","inputs":{"custom_tag":"${{ needs.sync-and-trigger.outputs.new_tag }}"}}'
      - name: Log if no tag was available to trigger
        if: needs.sync-and-trigger.outputs.new_tag == ''
        run: |
          echo "Warning: No tag was extracted, so the Docker build workflow was not triggered."
